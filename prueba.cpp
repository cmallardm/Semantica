#include <iostream>
#include <stdio.h>
#include <conio.h>
float area, radio, pi, resultado;
int a, d;
char x;
int y;
// Este programa calcula el volumen de un cilindro.
void main()
{
    scanf("%d", &x);
    y = 256;
    x = (char)(y);
}