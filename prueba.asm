#make_COM#
include emu8086.inc
ORG 100h
PRINTN "Introduce la altura de la piramide: "
CALL SCAN_NUM
MOV if, CX
MOV AX,altura
PUSH AX
MOV AX,2
PUSH AX
POP AX
POP BX
CMP AX, BX
JLE if0
MOV AX,altura
PUSH AX
POP AX
MOV i, AX
inicioFor1:
MOV AX,i
PUSH AX
MOV AX,0
PUSH AX
POP AX
POP BX
CMP AX, BX
JLE finFor1
MOV AX,1
PUSH AX
MOV AX,0
PUSH AX
POP AX
MOV j, AX
MOV AX,j
PUSH AX
MOV AX,altura
PUSH AX
MOV AX,i
PUSH AX
POP AX
POP BX
SUB AX, BX
PUSH AX
POP AX
POP BX
CMP AX, BX
JGE WHILE0:
MOV AX,j
PUSH AX
MOV AX,2
PUSH AX
POP AX
POP BX
DIV BX
PUSH DX
MOV AX,0
PUSH AX
POP AX
POP BX
CMP AX, BX
JNE if1
JMP Fin2
else2:
PRINTN "-"
JMP Fin2
if1:
JMP Fin else2
Fin 2:
MOV AX,1
PUSH AX
POP AXADD j, AXMOV j, AX
